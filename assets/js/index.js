
// Initialize script when all document body is loaded
document.addEventListener('DOMContentLoaded', init(window, document))

/**
 * Initiates the JavaScript functions
 * @param {Window} window 
 * @param {Document} document 
 */
const init = (window, document) => {
    const win = window;
    const doc = document;
}